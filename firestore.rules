rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to the 'pages' collection for any authenticated user.
    match /pages/{pageId} {
      allow read, write: if request.auth != null;
    }
  }
}
