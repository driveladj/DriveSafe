# خطة تنفيذ نظام إدارة المحتوى (CMS) ولوحة تحكم المتدربين

## الهدف الأساسي
بناء نظام شامل يتكون من جزأين رئيسيين:
1.  **لوحة تحكم إدارية (CMS):** تمنح مدير الموقع القدرة على التحكم الكامل في جميع جوانب الموقع بشكل مرئي وتفاعلي.
2.  **لوحة تحكم للمتدرب:** توفر للمتدربين المسجلين تجربة مخصصة وتسهل عليهم متابعة تقدمهم والتفاعل مع الأكاديمية.

---

## التقنيات المقترحة
- **الواجهة الأمامية:** Next.js, React, Tailwind CSS (المكدس التقني الحالي).
- **الخلفية وقاعدة البيانات:** Firebase (Firestore, Authentication, Cloud Functions) لتخزين البيانات، تأمين النظام، وإرسال الإشعارات.
- **التحقق من الهاتف:** Firebase Authentication (Phone Number with OTP).

---

## مراحل التنفيذ

### المرحلة الأولى: إعداد البنية التحتية (Firebase)
**الهدف:** تجهيز قاعدة بيانات مرنة لتخزين كل محتوى الموقع، بيانات المستخدمين، وإعدادات النظام.

1.  **دمج Firebase:**
    -   تكوين مشروع Firebase وربطه بالتطبيق.
    -   تفعيل خدمات Firestore (لقاعدة البيانات), Authentication (لتسجيل الدخول), و Cloud Functions (للعمليات الخلفية).

2.  **تصميم بنية قاعدة البيانات (Firestore):**
    -   **مجموعة `content`:** لتخزين محتوى الصفحات (نصوص، صور، أنماط). كل مستند يمثل صفحة أو قسماً قابلاً للتعديل.
    -   **مجموعة `users`:** لتخزين بيانات المتدربين (الاسم، الهاتف، الدورات المسجلة، حالة التأكيد).
    -   **مجموعة `courses` و `offers`:** لإدارة الدورات والعروض بشكل منفصل.
    -   **مجموعة `announcements`:** لتخزين الإعلانات الجديدة.

3.  **تأمين النظام:**
    -   استخدام Firebase Authentication لإنشاء نظام تسجيل دخول للمدير والمتدربين (عبر رقم الهاتف وكلمة مرور/OTP).
    -   إضافة حقل `role` (صلاحية) في مستندات المستخدمين للتمييز بين `admin` و `trainee`.
    -   كتابة قواعد أمان Firestore (`firestore.rules`) لضمان أن كل مستخدم يمكنه الوصول وتعديل البيانات المصرح له بها فقط.

---

### المرحلة الثانية: تطوير لوحة تحكم المتدرب
**الهدف:** إنشاء واجهة مخصصة للمتدرب بعد تسجيل الدخول.

1.  **التحقق والتوجيه:**
    -   تفعيل التحقق من رقم الهاتف عبر OTP عند التسجيل.
    -   بعد تسجيل الدخول، يتم توجيه المتدرب إلى لوحة تحكمه الخاصة (`/dashboard`).
2.  **واجهة لوحة التحكم:**
    -   عرض واجهة الموقع الرئيسية كما هي، مع إضافة شريط علوي أو قائمة جانبية تحتوي على:
        -   اسم المتدرب ورقم هاتفه.
        -   أيقونة إشعارات (لتنبيهه بالدورات أو الإعلانات الجديدة).
        -   زر تسجيل الخروج.
3.  **نظام الإشعارات:**
    -   عندما يضيف المدير إعلانًا أو دورة جديدة، يتم إنشاء إشعار في قاعدة البيانات.
    -   تظهر علامة على أيقونة الإشعارات في لوحة تحكم المتدرب. عند الضغط عليها، تعرض له قائمة بالإعلانات الجديدة.

---

### المرحلة الثالثة: تحويل الموقع إلى موقع ديناميكي
**الهدف:** تعديل صفحات الموقع لجلب محتواها من Firestore.

1.  **ربط المحتوى:**
    -   في كل صفحة، سيتم استبدال النصوص والصور والألوان الثابتة بالبيانات التي يتم جلبها من مجموعة `content` في Firestore.
    -   سيتم تطبيق الأنماط (الألوان، الخطوط) المحفوظة في قاعدة البيانات ديناميكيًا.

---

### المرحلة الرابعة: بناء لوحة التحكم الإدارية (CMS)
**الهدف:** إنشاء الواجهة التي سيستخدمها المدير لتعديل الموقع.

1.  **واجهة التحكم الرئيسية:**
    -   في صفحة `/admin`، عرض قائمة بالصفحات والعناصر القابلة للتعديل.
    -   إضافة أزرار تبديل (Toggle) لإظهار أو إخفاء الصفحات والعناصر.
2.  **أدوات التعديل:**
    -   عند اختيار عنصر للتعديل، تظهر نماذج وحقول لتغيير:
        -   **المحتوى:** (نصوص، روابط، صور).
        -   **الأنماط:** منتقي ألوان (Color Picker) لتغيير لون النص والخلفية، وقوائم منسدلة لتغيير نوع الخط وحجمه.
3.  **إدارة البيانات الديناميكية:**
    -   إنشاء واجهات مخصصة لإدارة "الدورات"، "الأسعار"، "الأسئلة الشائعة"، و"المتدربين".
    -   ربط المتدربين بالدورات التي سجلو فيها.

---

### المرحلة الخامسة: إضافة ميزات متقدمة
**الهدف:** توسيع قدرات النظام وتحسين تجربة المدير.

1.  **إحصاءات متقدمة:**
    -   في لوحة تحكم المدير، عرض إحصاءات ورسوم بيانية حول:
        -   عدد المتدربين الجدد والنشطين.
        -   الدورات والعروض الأكثر تسجيلاً.
        -   نسب النجاح في الامتحانات.
2.  **النسخ الاحتياطي واستعادة التعديلات (Versioning):**
    -   عند كل عملية حفظ للتغييرات على المحتوى، يتم حفظ نسخة من الحالة السابقة في مجموعة فرعية (`history`).
    -   إضافة واجهة للمدير تتيح له استعراض التغييرات السابقة واستعادة أي نسخة منها (Undo).
3.  **حجز الحصص (مستقبلي):**
    -   تطوير نظام يسمح للمتدربين بحجز مواعيد الحصص العملية أونلاين بناءً على توفر المدربين.
