
# خطة تنفيذ نظام إدارة المحتوى (CMS) المتكامل

## الهدف الأساسي
بناء لوحة تحكم إدارية شاملة تمنح مدير الموقع القدرة على التحكم الكامل في جميع جوانب الموقع بشكل مرئي وتفاعلي، بما في ذلك:
- **إدارة المحتوى:** تعديل النصوص والصور والروابط في أي صفحة.
- **إدارة الهيكل:** إظهار أو إخفاء صفحات كاملة أو عناصر محددة داخل الصفحات.
- **إدارة التصميم:** تغيير الألوان، الخطوط (النوع، الحجم)، ألوان الخلفيات، والأنماط الأخرى لجميع العناصر.
- **إدارة البيانات:** التحكم في البيانات الديناميكية مثل الدورات، الأسعار، والأسئلة الشائعة.

---

## التقنيات المقترحة
- **الواجهة الأمامية:** Next.js, React, Tailwind CSS (المكدس التقني الحالي).
- **الخلفية وقاعدة البيانات:** Firebase (Firestore و Firebase Authentication) لتخزين المحتوى وتأمين لوحة التحكم.
- **التفاعل مع الواجهة:** React Hook Form لمعالجة نماذج التعديل.

---

## مراحل التنفيذ

### المرحلة الأولى: إعداد البنية التحتية (Firebase)
**الهدف:** تجهيز قاعدة بيانات مرنة لتخزين كل محتوى الموقع وإعداداته.

1.  **دمج Firebase:**
    -   طلب إعداد Firebase للمشروع لإنشاء مشروع Firebase جديد وتكوين Firestore و Authentication.
    -   إضافة إعدادات Firebase إلى ملفات المشروع لربطه بالخدمات السحابية.

2.  **تصميم بنية قاعدة البيانات (Firestore):**
    -   إنشاء مجموعة `pages` (صفحات): كل مستند في هذه المجموعة يمثل صفحة على الموقع (مثل: `home`, `about`, `contact`).
    -   سيحتوي كل مستند صفحة على:
        -   `isVisible`: (boolean) لتحديد ما إذا كانت الصفحة ظاهرة في قائمة التنقل أم لا.
        -   `title`: عنوان الصفحة (لأغراض SEO).
        -   `content`: كائن (object) يحتوي على بيانات كل قسم في الصفحة. على سبيل المثال، صفحة "من نحن" قد تحتوي على `heroSection`, `storySection`, `missionSection`.
    -   سيحتوي كل قسم على حقول للمحتوى القابل للتعديل:
        -   `title`: (string) عنوان القسم.
        -   `description`: (string) النص الوصفي.
        -   `imageUrl`: (string) رابط الصورة.
        -   `styles`: كائن يحتوي على إعدادات التصميم (مثل `backgroundColor`, `textColor`, `fontSize`).

3.  **تأمين لوحة التحكم:**
    -   استخدام Firebase Authentication لإنشاء نظام تسجيل دخول مخصص للمدير.
    -   تأمين الوصول إلى صفحة `/admin` بحيث لا يمكن لأحد الدخول إليها إلا المدير المسجل.
    -   كتابة قواعد أمان Firestore (`firestore.rules`) لضمان أن عمليات الكتابة والتعديل على البيانات لا تتم إلا من قبل المدير المصادق عليه.

---

### المرحلة الثانية: تحويل الموقع إلى موقع ديناميكي
**الهدف:** تعديل صفحات الموقع الحالية لجلب محتواها من Firestore بدلاً من أن تكون ثابتة في الكود.

1.  **تعديل الصفحات:**
    -   في كل صفحة (مثل `src/app/about/page.tsx`), سيتم تعديل الكود لجلب البيانات الخاصة بها من مستند الصفحة المقابل في Firestore عند تحميلها.
    -   سيتم استبدال النصوص والصور الثابتة بالبيانات التي تم جلبها من قاعدة البيانات.
    -   سيتم تطبيق الأنماط (الألوان، الخطوط) المحفوظة في حقل `styles` ديناميكيًا على العناصر.

2.  **مثال عملي (صفحة "من نحن"):**
    -   سيتم تعديل `src/app/about/page.tsx` ليقوم بالاتصال بـ Firestore وجلب المستند `pages/about`.
    -   سيتم عرض عنوان الصفحة بناءً على `pages/about/heroSection/title`.
    -   سيتم عرض نص القصة بناءً على `pages/about/storySection/description`.
    -   سيتم تطبيق لون خلفية القسم بناءً على `pages/about/storySection/styles/backgroundColor`.

---

### المرحلة الثالثة: بناء لوحة التحكم الإدارية
**الهدف:** إنشاء الواجهة التي سيستخدمها المدير لتعديل الموقع.

1.  **عرض هيكل الموقع:**
    -   في صفحة `/admin`, سيتم عرض قائمة بجميع صفحات الموقع (من مجموعة `pages`).
    -   بجانب كل صفحة، سيكون هناك زر لـ "تعديل" وزر تبديل (toggle) لـ "إظهار/إخفاء" الصفحة من قائمة التنقل.

2.  **واجهة تعديل الصفحة:**
    -   عند الضغط على "تعديل"، سيتم الانتقال إلى صفحة تعديل مخصصة (مثال: `/admin/edit/about`).
    -   ستعرض هذه الصفحة جميع الأقسام والعناصر القابلة للتعديل في صفحة "من نحن".
    -   بجانب كل عنصر نصي (عنوان، وصف)، سيكون هناك حقل إدخال لتغيير النص.
    -   بجانب كل عنصر (قسم، زر)، سيكون هناك أيقونة "تصميم" تفتح نافذة منبثقة (modal) تحتوي على:
        -   **منتقي الألوان (Color Picker):** لتغيير لون النص أو لون الخلفية.
        -   **خيارات الخط:** قائمة منسدلة لاختيار نوع الخط، وحقول لتحديد حجم الخط.
        -   **زر تبديل (Toggle):** لإظهار أو إخفاء العنصر بالكامل.

3.  **حفظ التغييرات:**
    -   بعد إجراء التغييرات، سيضغط المدير على زر "حفظ".
    -   سيتم إرسال البيانات المحدثة إلى Firestore وتحديث المستند المقابل.
    -   بفضل ديناميكية الموقع (المرحلة الثانية)، ستظهر التغييرات على الفور للمستخدمين.

---

### المرحلة الرابعة: إضافة ميزات متقدمة
**الهدف:** توسيع قدرات نظام إدارة المحتوى.

1.  **إدارة العناصر الديناميكية:**
    -   إنشاء واجهات مخصصة في لوحة التحكم لإدارة القوائم مثل "الدورات"، "الأسعار"، و "الأسئلة الشائعة".
    -   سيكون بإمكان المدير إضافة، تعديل، أو حذف أي عنصر من هذه القوائم بسهولة.

2.  **إعادة ترتيب الأقسام:**
    -   إضافة وظيفة السحب والإفلات (Drag and Drop) في صفحة تعديل الصفحة للسماح للمدير بإعادة ترتيب الأقسام.
